import 'package:flutter/material.dart';

class TimesheetCalendarListWidget extends StatelessWidget {
  const TimesheetCalendarListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
